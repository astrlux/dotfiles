#!/bin/sh
#
# X window server
$HOME/.local/bin/screen &

# Initialize status bar.
$HOME/.config/dwm-bar/dwm_bar.sh &

# Set wallpaper.
$HOME/.fehbg &

# User programs
xcompmgr -f -c -C -t-5 -l-5 -o.55 -D6 &
firefox &
